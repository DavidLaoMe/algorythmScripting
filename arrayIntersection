/*
Construct a function intersection that compares input arrays and returns 
a new array with elements found in all of the inputs. BONUS: Use reduce!
*/

const range = [[5, 10, 15, 20], [15, 88, 1, 5, 7], [1, 10, 15, 5, 20]];

var intersection = range.reduce(function(accumulator, currentValue, currentIndex) {
  
  return accumulator.filter(function(item, index) {
    
    if(currentValue.includes(item)) {
      
    }
  });
});
